# Core dependencies
aiohttp>=3.8.0
pyyaml>=6.0.0
pandas>=2.0.0
numpy>=1.24.0

# API clients
injective-py>=0.5.0  # Replace with actual package name
allora-client>=0.1.0  # Replace with actual package name

# Testing
pytest>=7.0.0
pytest-asyncio>=0.20.0 